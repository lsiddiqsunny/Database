--solution 7.1
a. (SELECT EMPLOYEE_ID
FROM EMPLOYEES)
MINUS
(SELECT MANAGER_ID
FROM EMPLOYEES)
;

b. SELECT LAST_NAME
FROM EMPLOYEES
WHERE EMPLOYEE_ID=ANY((SELECT EMPLOYEE_ID
FROM EMPLOYEES)
MINUS
(SELECT MANAGER_ID
FROM EMPLOYEES))
;

c. ((SELECT LOCATION_ID
FROM LOCATIONS )
MINUS
(SELECT LOCATION_ID
FROM DEPARTMENTS))
;
